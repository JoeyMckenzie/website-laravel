# Code Quality

- ALWAYS use `composer lint:agent` for running phpstan analysis
- ALWAYS use `composer refactor:check` for potential rector refactorings
- ALWAYS use `composer fmt:check` for potential pint formatting issues
- ALWAYS run `composer check` to run all code quality checks at once
